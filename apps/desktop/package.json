{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "dev": "wait-on http://localhost:3000 && electron .",
    "build": "tsc --noEmit",
    "check-types": "tsc --noEmit",
    "lint": "eslint src --fix",
    "test": "echo 'No tests yet' && exit 0"
  },
  "dependencies": {
    "electron": "^38.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "globals": "^16.3.0",
    "typescript": "^5"
  }
}
