{
  "name": "arc",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20.9.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "start": "pnpm --filter desktop start",
    "package": "pnpm --filter desktop package",
    "make": "pnpm --filter desktop make",
    "publish": "pnpm --filter desktop publish",
    "lint": "pnpm --filter desktop lint",
    "db:dev": "pnpm --filter desktop db:dev",
    "db:release": "pnpm --filter desktop db:release"
  }
}
